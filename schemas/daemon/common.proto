syntax = "proto3";

// TODO: extend with additional options
message HyperdriveOptions {
  bytes key = 1;
  uint64 version = 2;
  bytes hash = 3;
  bool sparse = 4;
  bool sparseMetadata = 5;
  bool seed = 6;
}
